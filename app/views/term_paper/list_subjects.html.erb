<% content_tag :ul, :class => "group_subjects" do %>
  <% @subjects.each do |s| %>
    <% content_tag_for :li, s do %>
      <%= link_to s.subject.name, :action => "list_students", :id => s.id %>
    <% end %>
  <% end %>
<% end %>
